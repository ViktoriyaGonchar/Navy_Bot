# NaviBot - Desktop Web Wrapper

Легкое кроссплатформенное десктопное приложение-обёртка на Tauri, имитирующее нативное поведение на Windows 11.

## Возможности

- 🚀 Легковесное приложение на Tauri (не Electron)
- 🎨 Современный градиентный дизайн с темно-синим фоном
- 🤖 Стартовый экран с анимированным роботом
- 🌐 Встроенный веб-движок для просмотра сайтов
- ⚙️ Настройка стартовой страницы через конфиг-файл
- 📦 Автоматическая сборка инсталлятора

## Требования

- **Rust** (установите через [rustup.rs](https://rustup.rs/))
- **Node.js** (версия 16+)
- **System dependencies**:
  - Windows: Microsoft Visual C++ Build Tools
  - Для других платформ см. [Tauri prerequisites](https://tauri.app/v1/guides/getting-started/prerequisites)

## Быстрый старт

**Подробная инструкция по установке и запуску находится в файле [`ЗАПУСК.md`](ЗАПУСК.md)**

### Краткая инструкция:

1. Установите Rust через [rustup.rs](https://rustup.rs/)
2. Установите Node.js (версия 16+)
3. Установите Tauri CLI: `cargo install tauri-cli --version "^1.0"`
4. Установите расширения VSCode (см. `requirements.md`)
5. Запустите: `cd src-tauri && cargo tauri dev`

## Установка расширений VSCode

См. файл `requirements.md` для списка необходимых расширений.

## Сборка для production

```bash
# Сборка приложения
cargo tauri build
```

После сборки инсталлятор будет находиться в папке `src-tauri/target/release/bundle/`.

## Конфигурация стартовой страницы

Конфиг-файл автоматически создаётся при первом запуске в:
- **Windows**: `%APPDATA%\navibot\config.json`
- **Linux**: `~/.config/navibot/config.json`
- **macOS**: `~/Library/Application Support/navibot/config.json`

Пример `config.json`:
```json
{
  "start_url": "https://www.google.com"
}
```

Вы можете изменить `start_url` на любой другой URL.

## Структура проекта

```
NaviBot/
├── src/                 # Frontend (HTML/CSS/JS)
│   ├── index.html
│   ├── styles.css
│   └── main.js
├── src-tauri/           # Backend (Rust)
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   ├── build.rs
│   └── src/
│       └── main.rs
├── requirements.md      # Расширения VSCode
└── README.md           # Этот файл
```

## Иконка приложения

Иконка приложения (стилизованный робот 🤖 в круге) должна быть размещена в папке `src-tauri/icons/`:
- `32x32.png`
- `128x128.png`
- `128x128@2x.png`
- `icon.icns` (macOS)
- `icon.ico` (Windows)

## Лицензия

MIT

Made by Viktoriya Gonchar
